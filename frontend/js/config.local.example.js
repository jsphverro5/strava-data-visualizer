// ── Local config overrides ────────────────────────────────────────────────────
// Copy this file to `config.local.js` (same folder) and fill in your own values.
// config.local.js is gitignored, so your token stays private.
//
// Any variable you set here overrides the default in config.js.

// Your free MapBox public token from https://account.mapbox.com
// Tip: restrict it to http://localhost/* in the token settings.
MAPBOX_TOKEN = "pk.YOUR_OWN_TOKEN_HERE";

// Center the map on your region [longitude, latitude]
MAP_CENTER = [-105.2705, 40.0150]; // Boulder, CO
MAP_ZOOM   = 12;
